export enum SortOrder {
  DIRECT = 'Direct',
  REVERSE = 'Reverse',
}

export enum FilteredByDate {
  TODAY = 'Today',
  ALL_TIME = 'All_time',
}

export enum SortedBy {
  TITLE = 'Title',
  DATE = 'Date',
}
