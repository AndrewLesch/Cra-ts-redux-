export enum SortOrder {
  ASC = 'Ascending',
  DESC = 'Descending',
}

export enum FilteredByDate {
  TODAY = 'Today',
  ALL_TIME = 'All_time',
}

export enum SortedBy {
  TITLE = 'Title',
  DATE = 'Date',
}
